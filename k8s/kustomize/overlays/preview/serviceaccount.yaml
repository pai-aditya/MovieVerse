# The backend Deployment + migrate Job run as this SA. Each preview namespace
# needs its own copy (the shared rbac.yaml lives only in 'movieverse').
apiVersion: v1
kind: ServiceAccount
metadata:
  name: movieverse-backend
  namespace: movieverse # re-namespaced to mv-<slug> by the ApplicationSet
